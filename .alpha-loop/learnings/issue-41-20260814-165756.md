# Issue #41 interruption: Alpha Loop worktree reused the stable Compose project

- **Status:** Interrupted before issue planning or implementation.
- **Attempt:** Started epic #40 after the operator explicitly labeled #41–#49 `ready` and the exact dry run validated nine ordered batches.
- **Stop signal:** Session-worktree setup created `.env` with `COMPOSE_PROJECT_NAME=answer-engine-oss`, matching the live stable PostgreSQL and Redis project.
- **Safety result:** The runner was interrupted before queue discovery. No Docker Compose command ran; stable container identities stayed unchanged and the stable health endpoint remained HTTP 200.
- **Root cause:** Alpha Loop 2.4.0 derives the worktree Compose identity from the repository basename, while the repository had no setup guard to replace generated runtime settings before the worker starts.
- **Regression coverage:** The repository posture test now creates a real linked-worktree fixture, reproduces the colliding generated value, proves replacement with an isolated staging home/project/ports/credentials, proves idempotency and private secret files, and proves stable project, home, and port collisions fail closed.
- **Additional learning:** Context refresh also removed critical browser, PR-only, learning-file, and no-direct-master-push instructions. Those guards must remain asserted in repository tests.
- **Verification:** The final focused repository posture suite passed ten consecutive robustness runs (11/11 each). `pnpm install --frozen-lockfile` was already up to date. `pnpm verify` passed all boundary, lint, type-check, test, and build gates: 340 tests passed and the existing database integration test was skipped. The untouched stable API remained HTTP 200 and its container identities were unchanged. GitHub CI and a clean Alpha Loop bootstrap retry remain after merge.

Graphify remained excluded. No npm publication or direct `master` push occurred.
