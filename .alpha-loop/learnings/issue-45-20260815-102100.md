---
issue: 45
status: success
test_fix_retries: 1
date: 2026-08-15
retries: 0
---

## What Worked
- Reused the consent/reconciliation lifecycle from agent-history import while keeping folder inventory and retention semantics explicit.
- Split bounded metadata/binary discovery from full-byte archive/import so approval is an enforceable technical boundary.

## What Failed
- The first UI assertion used an unavailable matcher; checking the native disabled property kept the test independent of global matcher setup.
- A repeatable `--include` option with defaults silently broadened an explicit policy; leaving the Commander default unset makes provided globs replace the fallback set.
- Native `<summary>` controls were keyboard-focusable but missed the shared 3px focus rule until they were included explicitly.

## Patterns
- Bind consent to a channel-local manifest and server inventory, then restat before every approved read.
- Record excluded rows alongside candidates so final inventory reconciliation explains every discovered path.
- Keep preview cancellation visible before approval so a waiting local process has a clear terminal outcome.

## Anti-Patterns
- Do not leave a legacy direct-sync path capable of reading a folder outside the permissioned workflow.

## Suggested Skill Updates
- Add preview/apply manifest reconciliation and explicit retention audits to local-ingestion review checklists.
